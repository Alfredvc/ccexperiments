---
description: Probe fixture command (user scope) for the claude-code-caching harness. Not a real command. Do not invoke.
---

# Probe Command 1 (user scope)

Placeholder body. This file exists only so the `~/.claude/commands/` directory
is non-empty at claude startup and the chokidar watcher has a target file
the test can append to.
